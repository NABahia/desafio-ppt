{
  "name": "ppt",
  "version": "1.0.0",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "parcel": "^2.14.4",
    "svgo": "^3"
  },
  "scripts": {
    "dev": "parcel serve ./src/index.html",
    "clean": "rm -rf build",
    "build": "npm run clean && parcel build ./src/index.html --dist-dir build --public-url /desafio-ppt",
    "deploy": "gh-pages -d build"
  }
}
